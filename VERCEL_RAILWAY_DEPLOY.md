# 🚀 Deploy: Frontend на Vercel + Backend на Railway

Оптимальная конфигурация для продакшн деплоя!

## 🎯 Преимущества этого подхода:

✅ **Vercel** - идеален для React фронтендов (автоматическая оптимизация, CDN, edge caching)  
✅ **Railway** - отлично для Python бэкендов (FastAPI, автоматический деплой)  
✅ **Бесплатно** - оба сервиса дают бесплатный тариф для старта  
✅ **Автоматические деплои** - при каждом push в GitHub  

---

## 📋 Часть 1: Backend на Railway

### Шаг 1: Создайте проект в Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
4. Выберите репозиторий: `Perfect29/resp`

### Шаг 2: Настройте Backend сервис

Railway автоматически определит Python проект. Если нет:

- **Root Directory**: оставьте пустым (корень репозитория)
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

### Шаг 3: Добавьте Environment Variables

В разделе **Variables** сервиса добавьте:

```env
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT=30.0
PORT=8000
```

### Шаг 4: Дождитесь деплоя и получите URL

1. Railway автоматически начнет деплой
2. После успешного деплоя в разделе **Settings** → **Generate Domain**
3. Скопируйте URL бэкенда (например: `https://resp-backend.up.railway.app`)
4. **ВАЖНО**: Сохраните этот URL для настройки фронтенда!

### Шаг 5: Проверьте Backend

Откройте в браузере:
```
https://your-backend-url.railway.app/health
```

Должно вернуть: `{"status":"healthy"}`

---

## 📋 Часть 2: Frontend на Vercel

### Шаг 1: Подключите репозиторий к Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New..."** → **"Project"**
4. Импортируйте репозиторий: `Perfect29/resp`

### Шаг 2: Настройте проект

Vercel автоматически определит Vite проект. Настройте:

- **Framework Preset**: Vite (автоматически)
- **Root Directory**: `frontend` ⚠️ **ВАЖНО!**
- **Build Command**: `npm run build` (или оставьте по умолчанию)
- **Output Directory**: `dist` (по умолчанию)

### Шаг 3: Добавьте Environment Variables

В разделе **Environment Variables** добавьте:

```env
VITE_API_URL=https://your-backend-url.railway.app
```

⚠️ **Замените** `your-backend-url.railway.app` на реальный URL из Railway!

### Шаг 4: Деплой

1. Нажмите **"Deploy"**
2. Vercel автоматически:
   - Установит зависимости
   - Соберет проект
   - Задеплоит на CDN
3. Дождитесь завершения (обычно 1-2 минуты)

### Шаг 5: Получите домен

Vercel автоматически даст вам домен:
- Например: `resp.vercel.app`
- Или можно настроить кастомный домен

---

## 🔧 Настройка CORS на Backend

После деплоя обновите CORS в `app/main.py` для работы с Vercel:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://resp.vercel.app",  # Ваш Vercel домен
        "https://*.vercel.app",      # Все Vercel домены
        "http://localhost:5173",    # Локальная разработка
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

Или пока можете оставить `allow_origins=["*"]` для тестирования.

---

## 🎯 Структура после деплоя:

```
Frontend (Vercel):
https://resp.vercel.app
  ↓ API calls
Backend (Railway):
https://resp-backend.up.railway.app
```

---

## 🔄 Автоматические обновления

### После изменений в коде:

1. **Запушьте изменения в GitHub:**
   ```bash
   git add .
   git commit -m "Update code"
   git push
   ```

2. **Vercel и Railway автоматически:**
   - Обнаружат изменения
   - Пересоберут проект
   - Задеплоят новую версию

3. **Деплой займет 1-3 минуты**

---

## 🔍 Проверка деплоя

### Backend:
```bash
curl https://your-backend.railway.app/health
# Должно вернуть: {"status":"healthy"}
```

### Frontend:
- Откройте: `https://your-project.vercel.app`
- Должна открыться главная страница
- Попробуйте создать target и запустить анализ

---

## 🐛 Troubleshooting

### Frontend не может подключиться к Backend:

1. **Проверьте `VITE_API_URL`:**
   - В Vercel → Settings → Environment Variables
   - Должен быть полный URL с `https://`

2. **Проверьте CORS:**
   - Убедитесь что в `app/main.py` разрешен ваш Vercel домен
   - Или временно используйте `allow_origins=["*"]`

3. **Проверьте что Backend работает:**
   - Откройте `/health` endpoint
   - Проверьте логи в Railway

### Build ошибки на Vercel:

1. **"Module not found":**
   - Убедитесь что Root Directory: `frontend`
   - Проверьте что все зависимости в `package.json`

2. **"Build timeout":**
   - Vercel дает 45 секунд на бесплатном тарифе
   - Попробуйте оптимизировать зависимости

### Backend ошибки на Railway:

1. **"Port already in use":**
   - Используйте `$PORT` в команде запуска
   - Railway автоматически назначает порт

2. **"Module not found":**
   - Проверьте что все зависимости в `requirements.txt`
   - Посмотрите логи деплоя в Railway

---

## 💰 Стоимость

### Бесплатные тарифы:

**Vercel:**
- ✅ Бесплатный хостинг для фронтендов
- ✅ Автоматический SSL
- ✅ CDN по всему миру
- ✅ 100GB bandwidth в месяц

**Railway:**
- ✅ $5 бесплатного кредита каждый месяц
- ✅ Pay-as-you-go после
- ✅ Для MVP обычно достаточно бесплатного

---

## 📝 Чеклист перед деплоем:

- [ ] Репозиторий запушен в GitHub
- [ ] OPENAI_API_KEY готов
- [ ] Backend задеплоен на Railway
- [ ] Backend URL получен и работает (проверен `/health`)
- [ ] Frontend настроен на Vercel
- [ ] `VITE_API_URL` указывает на Railway backend
- [ ] CORS настроен на бэкенде
- [ ] Оба сервиса работают

---

## 🎉 Готово!

После успешного деплоя ваш проект будет:
- **Frontend**: Доступен на Vercel (быстро, с CDN)
- **Backend**: Работает на Railway (стабильно, с авто-деплоем)

Все готово к использованию! 🚀

